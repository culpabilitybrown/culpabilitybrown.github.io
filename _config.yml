defaults:
  -
    scope:
      path: ''

    values:
      layout: default

  -
    scope:
      type: posts
      path: blog

    values:
      layout: blost
      parent: blog

permalink: pretty

exclude: [vendor]
